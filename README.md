# Run a Laravel backend with a React client inside Docker
*This is not a template, but just represents my tinkering with the idea of using React with Laravel in conjunction with Docker and an EC2 instance.*

